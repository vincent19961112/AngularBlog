import { MustMatchDirective } from './Must-match.directive';

describe('NustMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new MustMatchDirective();
    expect(directive).toBeTruthy();
  });
});
